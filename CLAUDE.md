# CLAUDE.md

This is the Amanaska knowledge repository. Its entry point — navigation, document registry, documentation manifest, and the rules of the documentation system — is `00_Knowledge_Map.md`. Read it before any work.

How any role works here (human or AI): `Agents/Operating_Protocol.md`. Before opening a PR, run `python knowledge_index.py --check` and `python governance_check.py` (gates defined in `Process/Quality_Gates.md`).

This file exists only so Claude-family agents auto-load these pointers. The knowledge system is tool-agnostic; do not add content here.
